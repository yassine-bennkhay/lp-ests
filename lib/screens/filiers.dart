import 'package:flutter/material.dart';

class Filiers extends StatelessWidget {
  const Filiers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Filiers LP"),
    );
  }
}
